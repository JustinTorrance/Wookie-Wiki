import React from 'react';
import * as API from './APIcalls'

//probably won't need instance() since API calls isn't react??

describe('APIcalls', () => {
    describe('fetchText', () => {
        
    })
})
